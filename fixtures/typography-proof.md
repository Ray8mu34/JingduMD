# 混排校样：2026 年的中文阅读与 English Notes

这是匿名排版样张。正文同时包含中文、Latin words、0123456789、全角标点（《》、“”、：；）与**粗体术语**、*斜体变量*，用来检查同一行的字面大小和标点回退。读者应能沿着句子阅读，而不必辨认某个字体来自哪一套系统。

第二段继续正文节奏。标题和段落的停顿应清楚，段落之间仍有连接；单个 `inline token` 与行内变量 \(x_i\) 不应被误认为单独的按钮或卡片。

## 第一章：解释、公式与 2026 年的符号说明

先给出解释：当样本数增大，均值的定义保持简单，行内记号 \(\bar{x}\) 不改变整行的视觉节奏。由此得到：

\[
\bar{x}=\frac{1}{n}\sum_{i=1}^{n}x_i
\]

其中，\(n\) 是样本数，\(x_i\) 是第 \(i\) 个观测。公式前后的句子属于同一段推理，不需要被大块留白切断。

### 小节：从解释到推导

继续考虑两个量 \(a\) 和 \(b\)。以下列表将文字说明与公式保持在同一条阅读线上：

1. 先说明假设：\(a>0\)。

   \[
   f(a)=a^2
   \]

   因此这一步可以继续推导，而不必跳回列表外。
2. 再说明结果：\(b=f(a)\)，读者应能看出它仍属于第二项。

#### 四级标题：中西文 Mixed Heading

第五级和第六级标题仍应可辨，但无需彩色线条或展示型字号。

##### 五级标题：年份 2026 与全角标点

###### 六级标题：最后一层

这一层后的正文仍应有足够对比度，粗体 **Bold 术语** 和斜体 *italic word* 在 Windows 字体回退下需要可见区别。

## 一条足够长的混合标题：2026 年的解释、公式与符号说明，以及 A/B 测试的结果

长标题可以自然换行。它和下一段之间的距离应与章节入口的语义相称，不能因为多一行就形成过大的空白。

### 短代码与宽内容

短代码应留在阅读栏内：

```text
x = 1
```

下面的长代码需要在自己的容器中横向滚动，不要推动整页：

```typescript
const result = observations.map((value, index) => ({ index, value, explanation: "这一行用于检查很长的代码在窄窗中的可达性与复制结果" })).filter((item) => item.value > 0);
```

| 年份 | 解释 | 公式 | 备注 | 来源 | 校对 | 完整性 | 状态 | 最后一列 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 2026 | 保留作者的列对齐与原始内容 | \(x_i\) | 这一格有较长的中文说明，适合观察窄窗换行。 | 匿名样张 | 已核对 | 完整 | 可读 | 宽表末列可达 |

## 文末承接

结尾仍是普通正文。阅读、研读和四种纸色均应保留同一份内容能力。
