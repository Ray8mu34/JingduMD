# JingReader 阅读测试馆

这里的文档只用于开发与回归测试，不会打进正式安装包，也不会被应用写回。

- `reader-showcase.md`：标题、Callout、代码、公式、表格、脚注和本地图片。
- `security-cases.md`：危险协议、远程图片和损坏资源等安全边界。
- `assets/reading-grid.svg`：本地资源加载与打印排版夹具。
- `generated/`：由 `scripts/generate-stress-fixtures.ps1` 按需生成的十万字文档及万文件目录。

